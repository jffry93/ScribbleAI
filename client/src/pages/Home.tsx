const Home = () => {
	return (
		<div>
			<h1>Homepage</h1>
			<button
				onClick={() => {
					console.log('hello world');
				}}
			>
				Test button
			</button>
		</div>
	);
};

export default Home;
